# 2026 Vision Camp 등록 사이트 배포 가이드

이 폴더에는 3개의 파일이 있습니다.

- `index.html`, `styles.css`, `script.js` — 등록 신청 웹사이트 (반응형)
- `Code.gs` — 제출된 등록 정보를 구글 스프레드시트에 저장하는 Apps Script 코드

## 1단계. 기존 구글 스프레드시트 열기

이미 구글 폼과 연결된 스프레드시트가 있으므로 새로 만들지 않습니다.

1. 기존 스프레드시트를 엽니다: [2026 Vision Camp 등록 URL(응답)](https://docs.google.com/spreadsheets/d/1PTOEwLEV62BEZKd4jzCwIpXv36aRxWUucXsV37xRgqQ/edit)
2. 하단 탭이 `설문지 응답 시트1` 이고, 열 구성이 `타임스탬프 | 1. 이름 | 2. 캠퍼스 | 3. 학번 | 4. 연락처` 인지 확인합니다. (이미 그렇게 되어 있습니다.)
3. 지금은 "보기 전용"으로 열릴 수 있습니다 — 편집 권한이 있는 구글 계정으로 로그인되어 있는지 확인하세요.

## 2단계. Apps Script 연결하기

1. **반드시 위 스프레드시트 안에서** 메뉴의 **확장 프로그램 → Apps Script** 를 클릭합니다. (별도로 새 Apps Script 프로젝트를 만들면 안 됩니다 — 이 스프레드시트에 자동으로 연결되지 않습니다.)
2. 기본으로 열려있는 `Code.gs` 파일의 내용을 모두 지우고, 이 폴더의 `Code.gs` 내용을 붙여넣습니다.
3. 상단의 저장(💾) 버튼을 클릭합니다.

## 3단계. 웹 앱으로 배포하기

1. Apps Script 화면 오른쪽 위 **배포 → 새 배포** 를 클릭합니다.
2. 톱니바퀴(유형 선택) 아이콘을 눌러 **웹 앱** 을 선택합니다.
3. 설정을 다음과 같이 지정합니다.
   - **다음 사용자로 실행**: 나(본인 계정)
   - **액세스 권한이 있는 사용자**: 전체 공개(익명 사용자 포함)
4. **배포** 버튼을 클릭합니다.
5. 처음 배포 시 "승인 필요" 화면이 나오면 본인 계정으로 로그인 → "고급" → "안전하지 않은 페이지로 이동" 순서로 진행해 권한을 승인합니다. (본인이 만든 스크립트이므로 안전합니다.)
6. 배포가 완료되면 **웹 앱 URL** 이 표시됩니다. 이 URL을 복사해두세요.
   - 예: `https://script.google.com/macros/s/AKfycb.../exec`

## 4단계. 웹사이트에 URL 연결하기

1. `script.js` 파일을 엽니다.
2. 최상단의 다음 줄을 찾습니다.

   ```js
   const SCRIPT_URL = "YOUR_DEPLOYED_WEB_APP_URL_HERE";
   ```

3. `"YOUR_DEPLOYED_WEB_APP_URL_HERE"` 부분을 3단계에서 복사한 웹 앱 URL로 교체합니다.

   ```js
   const SCRIPT_URL = "https://script.google.com/macros/s/AKfycb.../exec";
   ```

4. 저장합니다.

## 5단계. 테스트하기

1. `index.html` 파일을 더블클릭해서 브라우저로 열거나, 아래처럼 로컬 서버로 실행합니다.

   ```bash
   cd "2026-vision-camp"
   python3 -m http.server 8080
   ```

   그 다음 브라우저에서 `http://localhost:8080` 접속.

2. 이름/캠퍼스/학번을 입력하고 "등록 완료하기"를 눌러봅니다.
3. 연결한 구글 스프레드시트에 새 행이 추가되는지 확인합니다.

## 6단계. 실제로 배포(공개)하기

정적 파일 3개(`index.html`, `styles.css`, `script.js`)만 있으면 되므로 아래 방법 중 편한 것을 사용하면 됩니다.

- **Netlify Drop**: [app.netlify.com/drop](https://app.netlify.com/drop) 에 폴더를 드래그하면 즉시 URL 발급
- **GitHub Pages**: 이 폴더를 GitHub 저장소에 올리고 Pages 기능 활성화
- **Vercel**: Vercel에 폴더 업로드 후 배포

배포 후 발급된 URL을 팀원/캠퍼스 순원들에게 공유하면 됩니다.

## 참고사항

- `설문지 응답 시트1` 탭 맨 아래에 제출시각, 이름, 캠퍼스, 학번, 연락처가 기존 응답들과 같은 열 순서로 이어서 기록됩니다.
- 기존 구글 폼을 계속 열어두면, 폼으로 들어오는 응답과 이 웹사이트로 들어오는 응답이 같은 시트에 섞여서 쌓입니다 (제출 시각 기준으로는 순서가 뒤섞일 수 있지만 데이터 자체는 문제 없습니다).
- Apps Script 코드를 수정한 뒤에는 **배포 → 배포 관리 → 수정(연필 아이콘) → 새 버전** 으로 다시 배포해야 변경사항이 반영됩니다. (URL은 그대로 유지됩니다.)
